-- SQL Comment

CREATE TABLE people (
    -- PRIMARY KEY tells SQL to use this variable to differentiate the data
    id INTEGER PRIMARY KEY,
    first_name TEXT,
    last_name TEXT,
    age INTEGER
);